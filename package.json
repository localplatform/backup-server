{
  "name": "backup-server",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "build": "npm run build:server && npm run build:client",
    "build:client": "npm run build -w client",
    "build:server": "cargo build --release -p backup-server-rs",
    "dev": "npm run build:client && pm2 start ecosystem.config.cjs",
    "start": "./target/release/backup-server"
  }
}
